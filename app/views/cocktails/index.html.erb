<body>
  <div class="bg">
    <div class="container">
      <div class="cards">
        <h1> A list of my favourite cockatils </h1>
              <% @cocktails.each do |cocktail| %>
                <%= link_to cocktail.name, cocktail_path(cocktail) %>
              <% end %>

      </div>

      <p> <%= link_to "Add a new Cocktail", new_cocktail_path, class: "btn-primary" %> </p>
    </div>
  </div>
</body>
